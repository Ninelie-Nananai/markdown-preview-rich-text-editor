import Head from "next/head";
import {
  Group,
  Container,
  Textarea,
  Grid,
  Center,
  Blockquote,
  Space,
  Title,
  Text,
} from "@mantine/core";
import { useState } from "react";
import { marked } from "marked";
import { GoMarkdown } from "react-icons/go";
import RichTextEditor from "../components/RichText";
import TimeLine from "../components/TimeLine";
import Report from "../components/Report";
import useTranslation from "next-translate/useTranslation";

export default function Home(props) {
  // const [colorScheme, setColorScheme] = useState("light");
  let { t } = useTranslation();

  const [markdown, setMarkdown] = useState(t("common:leave_comment"));

  return (
    <>
      <Container size="xl">
        <Head>
          <title>{t("common:web_title")}</title>
          <meta name="description" content="Generated by create next app" />
          <link rel="icon" href="/favicon.ico" />
        </Head>

        {/* Main section*/}
        <Group position="center">
          {/* Header */}
          <h1>
            <Title transform="uppercase">{t("common:title")}</Title>
            <Text color="red">{t("common:sub_title")}</Text>
          </h1>
        </Group>
        <Space h="xl" />
        {/* Body */}
        <Grid justify="space-around" align="flex-start" columns={2}>
          <Grid.Col span={1}>
            <div>
              <Text
                weight={600}
                transform="uppercase"
                align="center"
                component="span"
                variant="gradient"
                gradient={{ from: "indigo", to: "cyan", deg: 45 }}
                style={{ fontFamily: "Greycliff CF, sans-serif" }}
              >
                {t("common:header_one")}
              </Text>
              <Textarea
                placeholder={markdown}
                required
                autosize
                onChange={(e) => setMarkdown(e.target.value)}
              />
              <Group spacing={4}>
                <GoMarkdown size={20} />
                <Text size="sm" weight={400}>
                  {t("common:tip")}
                </Text>
              </Group>
            </div>
          </Grid.Col>
          <Grid.Col span={1}>
            <div>
              <Text
                weight={600}
                transform="uppercase"
                align="center"
                component="span"
                variant="gradient"
                gradient={{ from: "indigo", to: "cyan", deg: 45 }}
                style={{ fontFamily: "Greycliff CF, sans-serif" }}
              >
                {t("common:header_two")}
              </Text>
              <div className="post-body">
                <div
                  dangerouslySetInnerHTML={{ __html: marked(markdown) }}
                ></div>
              </div>
            </div>
          </Grid.Col>
        </Grid>

        <Space h="xl" />

        <Text
          weight={600}
          transform="uppercase"
          align="center"
          component="span"
          variant="gradient"
          gradient={{ from: "indigo", to: "cyan", deg: 45 }}
          style={{ fontFamily: "Greycliff CF, sans-serif" }}
        >
          Rich Text Editor
        </Text>
        <RichTextEditor style={{ minHeight: "350px" }} />
        <Space h="sm" />
        <Center inline>
          <Report />
        </Center>

        {/* Time line */}
        <Space h="xl" />
        <TimeLine />

        {/* Footer section */}
        <Space h="xl" />
        <Center>
          <Blockquote cite="– Nineria">{t("common:block_quote")}</Blockquote>
        </Center>
      </Container>
    </>
  );
}
